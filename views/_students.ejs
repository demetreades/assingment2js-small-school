<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link rel="preconnect" href="https://fonts.gstatic.com" />
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,300;0,400;0,600;1,300;1,400;1,600&display=swap" rel="stylesheet" />
  <link rel="icon" href="../public/images/favicon.png" />
</head>
<body>
  <div class="container">

    <nav>
      <div class="nav-class">
        <h1><%= title %></h1>
        <p>Welcome to <%= title %></p>
        <p>List of <%= title %></p>
      </div>
    </nav>

    <aside>
      <li><a href="/">home</a></li>
      <li><del><a href="/students">students</a></del></li>
      <li><a href="/students/new">add</a></li>
      
      <li><a href="/orders/">orders</a></li>
      <li><a href="/orders/new">add</a></li>
    </aside>

    <main>
      <ol>
        <div class="blue1"><p>List of <%= title %></p></div>
        <% if(studentsArray != undefined)  { %>
          <li><%= //studentsArray.data[0].student_first + " - " + studentsArray.data[0].student_last %></li>
          <% for(let i = 0; i < studentsArray.data.length; i++) { %>
              <li>
                <p>
                  <b><%= studentsArray.data[i].id %></b>
                  <%= studentsArray.data[i].student_first %>
                  <%= studentsArray.data[i].student_last %>
                  <%= studentsArray.data[i].hobby %>
                </p>
                <a class="success" href='/students/update/<%= studentsArray.data[i].id %>'>Update</a> 
                <a class="error" href='/students/delete/<%= studentsArray.data[i].id %>'>Delete</a>
              </li>
          <% } %>
        <% } %>  
        <div class="blue2"></div>
      </ol>
    </main>

    <footer>
      <p>
        small project , <a href="/" target="/">HOME</a>
      </p>
    </footer>

  </div>
  <!-- <script src="../public/javascripts/script.js"></script> -->
</body>
</html>
        